# Neuron Vision Display — RomeoFlexVision
# Google for Startups AI Agents Challenge 2026
#
# Deployment manifest for the full Streamlit app image (Dockerfile).
# Core library dependencies live in pyproject.toml — keep shared pins
# (anthropic, google-cloud-secret-manager, pydantic) in sync with it.

# ── Google Cloud / Vertex AI ─────────────────────────────────────────────────
google-cloud-aiplatform>=1.60.0
google-adk>=0.1.0
# Kept for the legacy roboqc_agent scaffold and ADK-compatible experiments.
# The deployed Neuron Vision Display path uses google-cloud-aiplatform/vertexai.
google-genai>=0.5.0

# ── AI / ML ──────────────────────────────────────────────────────────────────
vertexai>=1.60.0
# Claude Fable 5 reasoning engine (see docs/fable5_integration.md)
anthropic>=0.49.0,<1
google-cloud-secret-manager>=2.20.0,<3

# ── Web / API ────────────────────────────────────────────────────────────────
streamlit>=1.35.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── Data / validation ────────────────────────────────────────────────────────
pydantic>=2.7.0,<3
python-dotenv>=1.0.0

# ── Image processing ─────────────────────────────────────────────────────────
Pillow>=10.3.0

# ── Utilities ────────────────────────────────────────────────────────────────
httpx>=0.27.0
tenacity>=8.3.0
edge-tts>=6.1.0
pytest-asyncio>=0.23.0

# ── Observability / Arize Phoenix (partner track) ────────────────────────────
arize-phoenix>=4.0.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0
openinference-instrumentation-vertexai>=0.1.0
